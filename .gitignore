# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
.venv-ci/
env/

# OS / editor
.DS_Store
.idea/
.vscode/

# Streamlit
.streamlit/secrets.toml

# Local env (never commit secrets)
.env
.env.*

# Optional: keep personal portfolio config local (uncomment if you prefer)
# config.json
trade_journal.json
radar_hits.json
# Generated ticker dossiers (cached LLM/fundamentals output)
dossier_cache.json
dossier_cache.corrupt-*.json
*.corrupt-*

# Phase A ingest snapshots (WAL + shm)
data/snapshots.sqlite*
# Phase B web
node_modules/
web/.svelte-kit/
web/build/
web/dist/
web/.vite/
# Paper ledger (WAL)
data/paper_ledger.sqlite*
