# External imports (Python files only):
#   app.py          → streamlit, pandas, numpy, plotly
#   data/yf_engine.py → requests, yfinance, pandas, numpy
#
# Verified in this workspace’s default `python3`: numpy==2.0.2, requests==2.32.5.
# Streamlit was not installed in that interpreter. Replace the streamlit line with your
# runtime:  python3 -m pip show streamlit | grep '^Version:'
streamlit==1.39.1

yfinance==0.2.66
pandas==2.3.3
numpy==2.0.2
plotly==5.24.1
requests==2.32.5
# Institutional Quant Edge (HMM); without hmmlearn, A/B diagnostics fall back to retail pillars only
hmmlearn>=0.3.2
scipy>=1.11.0
